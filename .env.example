# Copy to .env.local. Never commit .env.local.
# Ask Adish for the real values.

NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# Service role key is NOT listed here on purpose.
# It bypasses row level security and must never enter application code.
# Adish keeps it locally for migrations only.
