# Copy to .env.local. Never commit .env.local.
# Ask Adish for the real values.

NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# Service role key is NOT listed here on purpose.
# It bypasses row level security. The ONE place application code may read it is
# src/lib/supabase/admin.ts (login-account administration only, D-023), so only Adish
# adds it, in his own .env.local. Never NEXT_PUBLIC_, never committed, never shared.
# SUPABASE_SERVICE_ROLE_KEY=
