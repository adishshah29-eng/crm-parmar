import { EmptyState } from "@/components/shared/states";

// STUB from Adish (Phase 0) so the shell has somewhere to land.
// Sayli replaces this file in Week 1 (task D1.1). Delete this comment when you do.
export default function DashboardPage() {
  return (
    <EmptyState
      title="Dashboard"
      hint="The company dashboard lands here in Week 1 (Sayli, task D1.1)."
    />
  );
}
