import { EmptyState } from "@/components/shared/states";

// STUB from Adish (Phase 0) so callers have somewhere to land after login.
// Tanishka replaces this file in Week 1 (task C1.1). Delete this comment when you do.
export default function MyDayPage() {
  return (
    <EmptyState
      title="My Day"
      hint="Your lead list lands here in Week 1 (Tanishka, task C1.1)."
    />
  );
}
